import { createTheme } from "@mui/material/";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#cc6600",
    },
    secondary: {
      main: "#e39d64",
    },
    text: {
      primary: "#543012",
    },
    background: {
      default: "#f5f5f5",
    },
  },
});
