import { createTheme } from "@mui/material/";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#6C4AB6",
    },
    secondary: {
      main: "#8D72E1",
    },
    background: {
      default: "#f5f5f5",
    },
  },
});
